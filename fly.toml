# fly.toml app configuration file generated for grrind-back on 2026-08-18T10:44:33Z
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'grrind-back'
primary_region = 'iad'

[build]

[env]
  # Sans ça, Caddy active l'auto-HTTPS sur le nom "localhost" et ne sert plus rien en
  # clair sur :80 (juste une redirection vers :443, jamais joignable) — `compose.yaml`
  # le fixe déjà pour le dev, il manquait ici. fly-proxy termine le TLS lui-même et
  # attend du HTTP en clair sur `internal_port`.
  SERVER_NAME = ':80'

[http_service]
  internal_port = 80
  force_https = true
  auto_stop_machines = true
  auto_start_machines = true
  min_machines_running = 0
  processes = ['app']

[[vm]]
  memory = '1gb'
  cpu_kind = 'shared'
  cpus = 1
  memory_mb = 256

[[vm]]
  memory = '512mb'
  cpu_kind = 'shared'
  cpus = 1
  memory_mb = 512
  processes = ['app']
  size = 'shared-cpu-1x'
