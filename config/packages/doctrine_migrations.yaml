doctrine_migrations:
    migrations_paths:
        # Namespace distinct d'App\ : les migrations ne doivent pas être autoloadées.
        'DoctrineMigrations': '%kernel.project_dir%/migrations'
    enable_profiler: false
